---
properties:
  availability:
    $ref: ../availability.yaml
  biblio_id:
    description: Record internal identifier
    type: integer
  item_availabilities:
    description: Availability of each item in this record
    items:
      $ref: ./item_availability.yaml
    type:
      - array
      - "null"
  items_total:
    description: Total item count
    type: integer
  items_checked:
    description: Checked item count
    type: integer
type: object
